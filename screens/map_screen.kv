<MapScreen>:
    canvas:
        #map component
        Rectangle:
            source: "assets/img/map_splash.png"
            size: self.size
            pos: self.pos

        #siteB component
        Rectangle:
            source: "assets/img/map_siteB.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/map_siteA.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/map_doors.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/map_tspawn.png"
            size: self.size
            pos: self.pos