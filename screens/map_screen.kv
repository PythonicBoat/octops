<MapScreen>:
    canvas.before:
        #map component
        Rectangle:
            source: "assets/img/map_map.png"
            size: self.size
            pos: self.pos

        #siteB component
        Rectangle:
            source: "assets/img/map_siteB.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/map_siteA.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/map_doors.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/map_spawn.png"
            size: self.size
            pos: self.pos
            on_touch_down: print("Buh")