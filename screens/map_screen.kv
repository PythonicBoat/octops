<MapScreen>:
    canvas.before:
        #map component
        Rectangle:
            source: "assets/img/map.png"
            size: self.size
            pos: self.pos

        #siteB component
        Rectangle:
            source: "assets/img/siteB.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/siteA.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/doors.png"
            size: self.size
            pos: self.pos

        Rectangle:
            source: "assets/img/spawn.png"
            size: self.size
            pos: self.pos